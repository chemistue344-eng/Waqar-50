Screenshot Checklist (place your screenshots here):
1. Cloud Postgres dashboard showing DB name & connection URL.
2. Cloud Web Service environment variables (JDBC_URL, DB_USER, DB_PASSWORD, PORT).
3. Successful build/deploy logs.
4. Browser hitting /health on your public URL (shows "ok").
5. POST /api/books in Postman or curl result, then GET /api/books list including your new record.
